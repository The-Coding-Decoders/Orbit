# Orbit
This is a modern alternative to shells like bash and zsh. This shell uses .pipe() and exec("command"). arg([argument1, argument2]). This shell has a oop syntax and supports file operations using fs module and got using Git module and uses LLVM Jit for speed.Note: This does not support posix.
